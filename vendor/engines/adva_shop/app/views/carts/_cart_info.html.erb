<% if !@cart.blank? and !@cart.cart_items.blank? %>
	<%= pluralize(@cart.cart_items.length, 'Item') %> in the cart.
	<%= link_to "View Cart", cart_path(@section, @cart) %>
<% else %>
	No Items
<% end %>