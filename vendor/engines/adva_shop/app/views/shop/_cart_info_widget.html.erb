<div id="cart_widget">Cart</div>
<div id="cart_widget1">
<% unless @cart.blank? %>
				<b><%= pluralize(@cart.cart_items.length, 'Item') %></b> in the cart.
				<%= link_to "View Cart", view_cart_path(@section) %>
			<% else %>
				No Items
			<% end %>
</div>

